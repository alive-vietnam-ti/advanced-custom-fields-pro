on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: alive-web.site
        username: alivevn
        password: ${{ secrets.ALIVEVN_PASSWORD }}
        server-dir: public_html/github/advanced-custom-fields-pro/
